import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#435072",
    },
    secondary: {
      main: "#606F84",
    },
  },
});
export default theme;
